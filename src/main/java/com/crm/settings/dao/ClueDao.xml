<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crm.settings.dao.ClueDao">
    <select id="selectActivity" resultType="com.crm.settings.domain.Activity">
        select a.id,a.name,a.startDate,a.endDate,u.name as owner
        from tbl_activity a join tbl_user u on a.owner = u.id where
        a.id in (select activityId from tbl_clue_activity_relation where clueId = #{clueId})
    </select>
    <insert id="insertBund">
        insert into tbl_clue_activity_relation values
        (#{id},#{clueId},#{activityId})
    </insert>
    <insert id="insert">
        insert into tbl_clue
        (id,
         fullname,
         appellation,
         owner,
         company,
         job,
         email,
         phone,
         website,
         mphone,
         state,
         source,
         createBy,
         createTime,
         description,
         contactSummary,
         nextContactTime,
         address)
        values (#{id},
                #{fullname},
                #{appellation},
                #{owner},
                #{company},
                #{job},
                #{email},
                #{phone},
                #{website},
                #{mphone},
                #{state},
                #{source},
                #{createBy},
                #{createTime},
                #{description},
                #{contactSummary},
                #{nextContactTime},
                #{address})
    </insert>
</mapper>